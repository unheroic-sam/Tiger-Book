import React from 'react';

const LiveFeed = () => {

	// state = {
	// 	posts: []
	// }

	// componentDidMount() {

	// 	fetch('/posts').then(stream => {
	// 		this.setState({
	// 			posts: stream.json();
	// 		});
	// 	})


	// }

	return(
		<div>
			<h1>LiveFeed</h1>
			{
				// posts.map(post => (
				// 	<h3>{post.title}</h3>
				// 	<span>{post.content}</span>
				// ));
			}
		</div>
	)
}

export default LiveFeed;